<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:weightSum="1"
    android:orientation="horizontal"
    android:padding="5dp"
    android:id="@+id/resumeTransactionViewHolder"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:alpha=".8"
        android:layout_weight=".6">

        <TextView
            android:id="@+id/labelResumeTransaction"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="@font/sfprotextbold"
            android:padding="10dp"
            android:textSize="10dp"
            android:text="#: "/>

    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_weight=".4">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnResumeTransaction"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:cornerRadius="5dp"
            app:backgroundTint="@color/acolor"
            android:layout_marginRight="5dp"
            app:icon="@drawable/baseline_play_arrow_24"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnUpdateCustomerName"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:cornerRadius="5dp"
            app:backgroundTint="@color/acolor"
            android:layout_marginRight="5dp"
            app:icon="@drawable/baseline_info_24"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnResumeTransactionBackout"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:cornerRadius="5dp"
            app:backgroundTint="@color/acolor"
            app:icon="@drawable/baseline_remove_shopping_cart_24"/>

    </LinearLayout>

</LinearLayout>